shm_id.ini 파일에서 key를 읽어서 shared momery 생성 및 접근

1번 thread 1?shm_item.ini 파일에서 data를 읽어서 shared memory에 작성 하고
shamred memory에 값 생성 시 구조체로 data 개수와 값을 설정하여 생성, 작성 끝났다는 출력이나 파일에서 읽어온 데이타 출력
   2. input: data 파일, shared memory 작성, 같은값 있으면 에러 출력


2번 thread 2?shared memory 의 data를 읽어서 화면 출력, shared memory 주기적으로 (1, 2sec) 감시하다가 변경된 데이타 있으면
변견된 데이타만 출력, 현재 data 개수

[lock 사용]

input 
thread1: aaaa

output 
thread2: aaaa 
